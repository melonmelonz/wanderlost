{
  "name": "wanderlost",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "bun test",
    "fetch-assets": "bun scripts/fetch-assets.mjs",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name=wanderlost --commit-dirty=true"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240914.0",
    "@happy-dom/global-registrator": "^15.7.0",
    "@preact/preset-vite": "^2.10.5",
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "wrangler": "^3.78.0"
  }
}
